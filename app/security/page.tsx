export const metadata = { title: "Security  Castra" }; export default function Page(){ const items = [ "OAuth & least-privilege scopes", "Encryption in transit & at rest", "Drafts-only by default (approval gate)", "Activity log & export", "Data deletion on request", "No data sold", "No training on your emails by default", "SOC 2 in progress", ]; return ( <div className="max-w-3xl mx-auto px-4 py-12"> <h1 className="font-serif text-3xl text-indigo mb-4">Security</h1> <ul className="list-disc pl-6 text-slate space-y-2"> {items.map(i=> <li key={i}>{i}</li>)} </ul> </div> ); }
