export async function listRecentDMs(_userId: string) {
  return []
}

export async function sendDM(_userId: string, _igUserId: string, _text: string) {
  // TODO: integrate IG later
  return { ok: false }
}
