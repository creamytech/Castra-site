import { useState } from "react"; export default function FAQ({ items }: { items: { q: string; a: string }[] }) { const [open, setOpen] = useState<number | null>(null); return ( <div className="max-w-2xl mx-auto space-y-4"> {items.map((item, i) => ( <div key={i} className="border border-gold/20 rounded-lg"> <button className="w-full px-4 py-3 text-left font-medium" onClick={() => setOpen(open === i ? null : i)}> {item.q} </button> {open === i && <div className="px-4 pb-3 text-slate">{item.a}</div>} </div> ))} </div> ); }
