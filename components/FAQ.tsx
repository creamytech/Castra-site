"use client"; import { useState } from "react"; export default function FAQ({ items }: { items: { q: string; a: string }[] }) { const [open, setOpen] = useState<number | null>(null); return ( <div className="max-w-2xl mx-auto space-y-4"> {items.map((item, i) => ( <div key={i} className="border border-gold/20 rounded-lg hover:border-gold/40 transition-colors"> <button className="w-full px-4 py-3 text-left font-medium hover:bg-gold/5 transition-colors" onClick={() => setOpen(open === i ? null : i)}> {item.q} </button> {open === i && <div className="px-4 pb-3 text-slate animate-fade-in">{item.a}</div>} </div> ))} </div> ); }
