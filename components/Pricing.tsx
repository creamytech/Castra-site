import { Card } from "@/components/ui/card"; import { Button } from "@/components/ui/button"; export default function Pricing({ title, subtitle, plan }: { title: string; subtitle: string; plan: { name: string; price: string; cta: string; bullets: string[] } }) { return ( <div> <div className="text-center max-w-2xl mx-auto mb-8"> <h2 className="font-serif text-3xl md:text-4xl text-indigo">{title}</h2> <p className="text-slate mt-2">{subtitle}</p> </div> <Card className="max-w-md mx-auto p-6 border border-gold/30 shadow-glow"> <div className="flex items-baseline justify-between"> <div> <div className="text-sm text-slate">{plan.name}</div> <div className="text-3xl font-serif text-indigo">{plan.price}</div> </div> <Button>{plan.cta}</Button> </div> <ul className="mt-4 grid gap-2 text-sm"> {plan.bullets.map((b) => ( <li key={b} className="flex items-center gap-2"><span className="inline-block h-1.5 w-1.5 rounded-full bg-[#D4AF37]" />{b}</li> )) } </ul> </Card> </div> ); }
