import { motion } from "framer-motion"; export default function TestimonialMarquee({ testimonials }: { testimonials: { quote: string; name: string; role: string }[] }) { return ( <div className="overflow-hidden"> <motion.div className="flex gap-8" animate={{ x: [0, -50] }} transition={{ duration: 20, repeat: Infinity, ease: "linear" }}> {[...testimonials, ...testimonials].map((t, i) => ( <div key={i} className="flex-shrink-0 w-80 bg-white/70 dark:bg-white/[0.05] backdrop-blur border border-gold/20 p-4 rounded-lg"> <p className="text-sm mb-3">"{t.quote}"</p> <div className="text-xs"> <div className="font-medium">{t.name}</div> <div className="text-slate">{t.role}</div> </div> </div> ))} </motion.div> </div> ); }
