"use client"; import { motion } from "framer-motion"; import { premiumBlobs } from "@/lib/gradients"; export default function GradientBG({ className = "" }: { className?: string }) { return ( <div className={`pointer-events-none absolute inset-0 -z-10 overflow-hidden ${className}`}> {premiumBlobs.map((b, i) => ( <motion.div key={i} className={`opacity-30 rounded-full bg-gradient-radial ${i===0 ? "via-[#2E2A47]" : "via-[#D4AF37]"}`} style={{ position: "absolute", ...b.style, filter: "blur(60px)" }} animate={{ y: [0, -15, 0], scale: [1, 1.03, 1] }} transition={{ duration: 16 + i * 2, repeat: Infinity }} /> ))} </div> ); }
